{
  "name": "website-with-ingyeom",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "npm run start --prefix backend",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix frontend && npm run sync:frontend-dist",
    "sync:frontend-dist": "node scripts/sync-frontend-dist.mjs",
    "postinstall": "npm install --prefix frontend && npm install --prefix backend",
    "install:all": "npm install && npm install --prefix frontend && npm install --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}